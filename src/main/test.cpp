#include <iostream>

#include "database.h"
#include "sort.hpp"
#include "str.hpp"
using std::cin;
using std::cout;
using std::endl;
using std::string;
int main() {
#ifndef ONLINE_JUDGE
  freopen("stdin.txt", "r", stdin);
  freopen("stdout.txt", "w", stdout);
#endif
  sjtu::bpTree<int, int> a("123");
  return 0;
}